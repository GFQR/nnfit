DATA      // COMMENT 
---------------------------------------------------------------------------------
N         // generate new TS? Y/N [bool ini_d.flag_genTS]
1000      // TS size [int ini_d.ts_size]
10        // mini-batch size [int ini_d.mb_size] < TS size
C         // choose fx: (A) quad, (B) linear (C) cosine [char ini_d.fx_choice]
1.7       // a [float ini_d.fx_a]
6.1       // b [float ini_d.fx_b]
1.0       // c [float ini_d.fx_c]
0.3       // learning rate [double ini_d.eta]
100       // epoch number [int ini_d.epoch_number]
0.0003    // delta, threshold value of C to stop grad-desc [double ini_d.delta]
-0.8768   // w_{00}^{(1)} layer 1 (hidden) weight 00
-0.0066   // w_{10}^{(1)}
0.4097    // w_{20}^{(1)} all three -> [float ini_d.wl1[3]]
-0.5467   // w_{00}^{(2)} layer 2 (output) weight 00
0.8846    // w_{01}^{(2)}
0.2603    // w_{02}^{(2)} all three -> [float ini_d.wl2[3]]
-0.4495   // b_0^{(1)} layer 1 bias [float ini_d.b0l1]
-0.4715   // b_1^{(1)}
0.8061    // b_2^{(1)} all three -> [float ini_d.bl1[3]]
0         // b_0^{(2)} layer 2 bias [float ini_d.bl2]
